# build output
dist/
# astro generated types & cache
.astro/
# dependencies
node_modules/
# env & local
.env
.env.*
!.env.example
.DS_Store
# tina generated files (regenerated by `tinacms build`/`tinacms dev`;
# the cloud build bakes a live auth token into client.ts, so this must
# never be committed)
tina/__generated__/
public/admin/
# logs
npm-debug.log*
.vercel
